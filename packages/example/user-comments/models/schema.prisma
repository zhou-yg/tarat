//
// @@tarat generated file
// provide by @tarat
// warning: auto generated by tarat.do not modifed this file
//
// original writing schema
datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["fullTextSearch", "fullTextIndex"]
}

// auto composing schema 
model Tarat_post_comments_Topic {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now())

  title String

  comments                     Tarat_post_comments_Comment[]
  tarat_user_login_system_User Tarat_user_login_system_User  @relation(fields: [author], references: [id])
  author                       String
}

model Tarat_post_comments_Comment {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now())

  name     String
  authorId Int

  content String

  likeCount Int? @default(0)

  topic   Tarat_post_comments_Topic @relation(references: [id], fields: [topicId])
  topicId Int

  replyCommentId Int?
  replyComment   Tarat_post_comments_Comment? @relation("reply", references: [id], fields: [replyCommentId])

  appendComments               Tarat_post_comments_Comment[] @relation("reply")
  tarat_user_login_system_User Tarat_user_login_system_User  @relation(fields: [author], references: [id])
  author                       String
}

model Tarat_user_login_system_User {
  id         String   @id @default(cuid())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now())

  name     String                                                    @unique
  password String
  avatar2  Tarat_user_login_system_Tarat_file_uploader_UploaderItem?
  topics   Tarat_post_comments_Topic[]
  comments Tarat_post_comments_Comment[]
}

model Tarat_user_login_system_SessionStore {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now())

  fromIndex String // paltform unique cookie id
  userId    String
}

model Tarat_user_login_system_Tarat_file_uploader_UploaderItem {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now())

  name   String
  link   String
  user   Tarat_user_login_system_User @relation(fields: [userId], references: [id])
  userId String                       @unique
}
