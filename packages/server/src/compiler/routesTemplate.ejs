/**
 * warning: this file is generated by tarat. do not modified this file
 */
import React from 'react'
import {
  Routes,
  Route,
} from "react-router-dom"
import {
  StaticRouter,
} from "react-router-dom/server"

import { setHookAdaptor, registerModelIndexes } from '@polymita/connect'

setHookAdaptor(React as any)

<%- entryCSSPath %>

<% if (rootPath) { %>
  import <%= rootName %> from '<%- rootPath %>'
<% } %>  

<%- imports %>

registerModelIndexes(<%- modelIndexes %>)

export default function _TaratRootApplication({
  location
}) {
  return (
    <StaticRouter location={location}>
      <%- rootStart %>
      <Routes>
        <%- routes %>
      </Routes>  
      <%- rootEnd %>
    </StaticRouter>
  );
}
