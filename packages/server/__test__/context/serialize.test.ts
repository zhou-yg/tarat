import {
  startTestServer
} from '../mockUtil'

describe('serialize', () => {
  let app
  beforeAll(async () => {
    // app = await startTestServer()
  })
  afterAll(() => {
    // app!.close()
  })

  it('send to server', async () => {
    
  })
})